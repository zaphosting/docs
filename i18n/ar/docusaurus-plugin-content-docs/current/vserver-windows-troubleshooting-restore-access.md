---
id: vserver-windows-troubleshooting-restore-access
title: "VPS: استعادة الوصول"
description: "اكتشف كيف تستعيد الوصول إلى VPS ويندوز الخاص بك بعد قفل كلمة المرور وتقليل وقت التوقف → تعلّم المزيد الآن"
sidebar_label: استعادة الوصول
services:
  - vserver
---

import InlineVoucher from '@site/src/components/InlineVoucher';

## مقدمة

ممكن يصير بسرعة... تحاول تسجل دخول لسيرفر ويندوز، لكن كلمة المرور ما عاد تُقبل أو نسيتها ببساطة. فجأة، يتم قفلك وما تقدر توصل للبيانات والتطبيقات المهمة. الوضع مزعج طبعًا، لكنه مش نهاية العالم. باتباع الخطوات اللي تحت، تقدر تستعيد الوصول لـ VPS ويندوز وتكمل شغلك بأقل تعطيل ممكن.

## التحضير
إعادة تعيين الوصول من حساب المسؤول (administrator) تتم باستخدام ملف ISO لويندوز. يعني لازم تستخدم نفس ملف الـ ISO اللي استُخدم أصلاً لتثبيت نظام تشغيل ويندوز على السيرفر.

تقدر تنفذ هالخطوة إما عن طريق [**التثبيت الأولي**](dedicated-setup.md) أو عبر **[ISO](dedicated-iso.md)**. اختار أو حدد ملف الـ ISO اللي استُخدم أصلاً، حسب الطريقة اللي تستخدمها.

ركّب ملف الـ ISO وأعد تشغيل النظام بعدها. المفروض تفتح واجهة تثبيت ويندوز مرة ثانية عند تشغيل النظام. النتيجة لازم تكون مثل هالصورة:

![](https://screensaver01.zap-hosting.com/index.php/s/XGKfQrwdcmcabY6/preview)

## استعادة الوصول

الحين جاء وقت إعادة تعيين وصول حساب المسؤول. اتبع أول خطوات التثبيت مرة ثانية، لكن توقف عند بداية التثبيت وروح لـ **إصلاح جهاز الكمبيوتر الخاص بك (Repair your computer)**.

![](https://screensaver01.zap-hosting.com/index.php/s/qwPgHyqNaQdsqzm/preview)

راح تفتح خيارات متقدمة. اضغط على خيار **استكشاف الأخطاء (Troubleshoot)** وبعدين على **موجه الأوامر (Command Prompt)**. راح يفتح لك نافذة موجه الأوامر (cmd.exe).

![](https://screensaver01.zap-hosting.com/index.php/s/BEan26iNkmzECJ3/download)

نفذ الأوامر التالية وحدة وحدة في موجه الأوامر:

```
d:
cd Windows
cd System32
move utilman.exe utilman.exe.bak
copy cmd.exe utilman.exe
net user administrator /active:yes
shutdown -r -t 0
```
:::warning

تخطيط لوحة المفاتيح في وحدة تحكم iLO HTML ممكن يختلف عن تخطيطك، فبعض الرموز ممكن تكون على أزرار مختلفة. خذ هالشي بعين الاعتبار عشان تنفذ الأوامر بشكل صحيح.

:::

بعد إعادة تشغيل السيرفر، في شاشة تسجيل الدخول الرئيسية، اضغط على تركيبة المفاتيح **Win+U**. في نافذة موجه الأوامر، اكتب التالي:

```
net user Administrator YourNewPassword
exit
```

![](https://screensaver01.zap-hosting.com/index.php/s/TiKJZPdg2kj5LG3/download)

كلمة المرور تغيرت الحين. لكن لازم ترجع تغييراتك على ملفات **Utilman**. عشان تسوي هالشي، بس أعد تشغيل السيرفر وكرر الخطوات. في موجه الأوامر، نفذ الأوامر التالية:

```
d:
cd Windows
cd System32
del utilman.exe ren utilman.exe.bak utilman.exe
shutdown -r -t 0
```

## الخلاصة

بعد ما تخلص هالعملية، المفروض تكون ضبطت كلمة مرور جديدة لحساب المسؤول بنجاح. تقدر تستخدمها الحين لتسجيل الدخول عبر اتصال سطح المكتب البعيد. لأي أسئلة أو مساعدة إضافية، لا تتردد تتواصل مع فريق الدعم عندنا، متوفرين يوميًا لمساعدتك! 🙂