---
id: dedicated-windows-rdp-port
title: "سيرفر مخصص: تغيير منفذ RDP في ويندوز"
description: "اكتشف كيفية تغيير منفذ RDP في ويندوز → تعلّم المزيد الآن"
sidebar_label: تغيير منفذ RDP
services:
  - dedicated
---

import InlineVoucher from '@site/src/components/InlineVoucher';



## مقدمة

بروتوكول سطح المكتب البعيد (RDP) يستخدم المنفذ 3389 بشكل افتراضي. هذا المنفذ معروف على نطاق واسع وغالبًا ما يكون هدفًا للمسح التلقائي. تغيير منفذ RDP يمكن أن يقلل من محاولات الاتصال غير المرغوب فيها ويحسن الأمان الأساسي.

<InlineVoucher />



## تغيير منفذ RDP في الريجستري

سجّل الدخول إلى السيرفر محليًا أو عبر اتصال سطح المكتب البعيد الحالي. افتح مربع التشغيل باستخدام **Win + R**، اكتب `regedit` واضغط موافق لفتح محرر الريجستري. انتقل إلى المسار التالي في الريجستري:

```
HKEY_LOCAL_MACHINE
 └─ SYSTEM
    └─ CurrentControlSet
       └─ Control
          └─ Terminal Server
             └─ WinStations
                └─ RDP-Tcp
```

ابحث عن الإدخال المسمى **PortNumber**. غيّر تنسيق القيمة إلى **عشري (Decimal)** وأدخل رقم المنفذ الجديد المطلوب. يُفضل اختيار منفذ أعلى من 1024 وغير مستخدم من قبل خدمة أخرى. بعد تطبيق التغيير، أغلق محرر الريجستري.

## تعديل قواعد جدار ناري ويندوز

يجب السماح للمنفذ الجديد في جدار ناري ويندوز. افتح **Windows Defender Firewall with Advanced Security** وانتقل إلى قسم القواعد الواردة.

قم بتعديل قاعدة سطح المكتب البعيد الحالية أو أنشئ قاعدة واردة جديدة تسمح بحركة مرور TCP على المنفذ الجديد. تأكد من أن القاعدة تسمح بالاتصال وتُطبق على ملفات تعريف الشبكة المطلوبة.

## إعادة تشغيل الخدمة لتطبيق التغييرات

يصبح تغيير المنفذ فعالًا فقط بعد إعادة تشغيل خدمات سطح المكتب البعيد أو إعادة تشغيل النظام. يُنصح بإعادة تشغيل السيرفر بالكامل لضمان تطبيق الإعدادات بشكل متسق.

## الاتصال باستخدام المنفذ الجديد

عند إنشاء اتصال جديد بسطح المكتب البعيد، يجب تحديد المنفذ صراحةً بإضافته إلى عنوان IP أو اسم المضيف. مثال:

```
203.0.113.10:3390
```

## الخلاصة

تغيير منفذ RDP في ويندوز يقلل من التعرض للمسح التلقائي ومحاولات الاتصال غير المصرح بها. تتضمن العملية تحديث الريجستري، السماح بالمنفذ الجديد في الجدار الناري، وإعادة تشغيل النظام أو الخدمات.

بعد الانتهاء، يجب التحقق من الوصول فورًا وتوثيق المنفذ المُعد لتجنب مشاكل الاتصال مستقبلًا. لأي أسئلة أو مساعدة إضافية، لا تتردد في التواصل مع فريق الدعم لدينا، المتوفر يوميًا لمساعدتك! 🙂

<InlineVoucher />