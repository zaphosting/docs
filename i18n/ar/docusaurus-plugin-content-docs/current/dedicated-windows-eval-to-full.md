---
id: dedicated-windows-eval-to-full
title: "سيرفر مخصص: تحويل نسخة تقييم Windows Server إلى النسخة الكاملة"
description: "اكتشف كيفية تحويل نسخة تقييم Windows Server إلى النسخة الكاملة → تعلّم المزيد الآن"
sidebar_label: تحويل التقييم إلى كامل
services:
  - dedicated
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import InlineVoucher from '@site/src/components/InlineVoucher';

## مقدمة

إصدارات تقييم Windows Server مصممة لأغراض الاختبار ومحدودة زمنياً. للاستمرار في استخدام نفس التثبيت في بيئة الإنتاج، يمكن تحويل نسخة التقييم إلى نسخة مرخصة بالكامل دون الحاجة لإعادة تثبيت نظام التشغيل.

تدعم مايكروسوفت هذا التحويل المباشر باستخدام أداة DISM، بشرط توفر مفتاح منتج صالح للإصدار المستهدف.

<InlineVoucher />

## المتطلبات الأساسية

قبل بدء التحويل، تأكد من أن لديك مفتاح منتج صالح لإصدار Windows Server الذي تريد التبديل إليه، مثل Standard أو Datacenter. يجب أن يتطابق مفتاح المنتج تماماً مع الإصدار المستهدف.

## الحصول على الإصدار الحالي

للتحقق من الإصدار المثبت حالياً، افتح موجه أوامر أو PowerShell بصلاحيات مرتفعة ونفذ الأمر التالي:

```powershell
DISM /Online /Get-CurrentEdition
```

سيعرض الناتج معرف الإصدار النشط. عادةً ما تظهر تثبيتات التقييم كـ `ServerStandardEval` أو `ServerDatacenterEval`.

## التحقق من الإصدارات المستهدفة المدعومة

ليس كل إصدار يمكن تحويله إلى أي إصدار آخر. لرؤية الإصدارات المرخصة المدعومة لتثبيتك الحالي، نفذ الأمر التالي:

```
DISM /Online /Get-TargetEditions
```

تمثل الإصدارات المدرجة الأهداف الصالحة التي يمكن استخدامها للتحويل.

## ترقية نسخة التقييم إلى النسخة الكاملة

بمجرد معرفة الإصدار المستهدف وتوفر مفتاح منتج صالح، يمكن بدء الترقية. يجب أن تتطابق قيمة `<TargetEdition>` مع أحد الإصدارات المدعومة التي تم إرجاعها من فحص إصدارات DISM.

الإصدارات المستهدفة الشائعة تشمل:
- `ServerStandard`
- `ServerDatacenter`

استخدم الأمر التالي لبدء التحويل. استبدل `<TargetEdition>` بالإصدار المطلوب و `<ProductKey>` بمفتاح منتج Windows Server المكون من 25 حرفًا:

```
DISM /Online /Set-Edition:<TargetEdition> /ProductKey:<ProductKey> /AcceptEula
```

| إصدار نظام التشغيل           | مفتاح ترخيص الحجم العام          |
| ---------------------------- | ------------------------------- |
| Windows Server 2025 Standard  | TVRH6-WHNXV-R9WG3-9XRFY-MY832  |
| Windows Server 2025 Datacenter| D764K-2NDRG-47T6Q-P8T8W-YP6DF  |
| Windows Server 2022 Standard  | VDYBN-27WPP-V4HQT-9VMD4-VMK7H  |
| Windows Server 2022 Datacenter| WX4NM-KYWYW-QJJR4-XV3QB-6VM33  |

خلال العملية، قد يبدو أن التقدم يتوقف لفترة قصيرة. هذا السلوك طبيعي. بعد اكتمال التحويل، يلزم إعادة تشغيل لإتمام تغيير الإصدار.

## التحقق من إصدار Windows Server

بعد إعادة تشغيل السيرفر، افتح موجه أوامر أو PowerShell بصلاحيات مرتفعة ونفذ:

```powershell
DISM /Online /Get-CurrentEdition
```

يجب أن يعرض الناتج الآن الإصدار المرخص، مثل `ServerStandard` أو `ServerDatacenter`، مما يؤكد نجاح تحويل نسخة التقييم.

في هذه المرحلة، سترى علامة مائية **تفعيل Windows** في الزاوية اليمنى السفلى من الشاشة. يمكنك الآن استخدام ترخيص Windows Server الذي اشتريته لتفعيل إصدار Windows Server Standard أو Datacenter.

## الخاتمة

مبروك! لقد نجحت الآن في تحويل نسخة Windows Server الخاصة بك من EVAL إلى النسخة الكاملة. لأي أسئلة أو مساعدة إضافية، لا تتردد في التواصل مع فريق الدعم الخاص بنا، المتوفر يومياً لمساعدتك! 🙂


<InlineVoucher />