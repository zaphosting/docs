---
id: dedicated-linux-troubleshooting-restore-access
title: "سيرفر مخصص: استعادة الوصول"
description: "اكتشف كيف تستعيد الوصول إلى سيرفر Linux المخصص الخاص بك بعد قفل كلمة المرور وتقليل وقت التوقف → تعلّم المزيد الآن"
sidebar_label: استعادة الوصول
services:
  - dedicated
---

import InlineVoucher from '@site/src/components/InlineVoucher';

## مقدمة

يمكن أن يحدث ذلك بسرعة. تحاول تسجيل الدخول إلى سيرفر Linux الخاص بك، لكن كلمة المرور لم تعد مقبولة أو تم نسيانها. ونتيجة لذلك، يصبح الوصول إلى النظام والبيانات أو الخدمات الحيوية مستحيلاً. يمكن أن تكون هذه الحالة محبطة، خاصةً عندما يكون السيرفر مطلوبًا للعمليات الجارية. في معظم توزيعات Linux، يمكن استعادة الوصول باستخدام وضع الاسترداد دون الحاجة لإعادة تثبيت نظام التشغيل. باتباع الخطوات أدناه، يمكنك استعادة الوصول إلى سيرفرك ومواصلة العمل بأقل قدر من الانقطاع.

## استعادة الوصول

لإعادة تعيين كلمة المرور باستخدام وضع الاسترداد، يجب تشغيل النظام مع الوصول إلى قائمة إقلاع GRUB. أثناء بدء تشغيل النظام، افتح قائمة GRUB واختر **خيارات متقدمة لأوبونتو**.

![img](https://screensaver01.zap-hosting.com/index.php/s/j6DKWdM95fFWWZi/preview)

في الشاشة التالية، اختر الإدخال الذي يبدأ النظام في **وضع الاسترداد**. سيؤدي ذلك إلى تحميل بيئة الاسترداد بدلاً من نظام التشغيل العادي.

بمجرد عرض قائمة الاسترداد، اختر **root – الدخول إلى موجه الأوامر بصلاحيات root** وقم بتأكيد الاختيار. اضغط Enter مرة أخرى للوصول إلى موجه root.

![img](https://screensaver01.zap-hosting.com/index.php/s/WFjTj3FxKQwHemD/preview)

في وضع الاسترداد، يتم تركيب نظام الملفات root كقراءة فقط بشكل افتراضي. للسماح بتغيير كلمة المرور، يجب إعادة تركيبه مع صلاحيات الكتابة. نفذ الأمر التالي:

```bash
mount -o remount,rw /
```

إذا كان اسم المستخدم غير معروف، يمكن سرد جميع حسابات المستخدمين المتاحة بفحص مجلدات المنزل:

```
ls /home
```

لإعادة تعيين كلمة المرور، استخدم أمر `passwd` متبوعًا باسم المستخدم:

```
passwd username
```

استبدل `username` باسم الحساب الفعلي. أدخل كلمة المرور الجديدة و أكدها عند الطلب. لن يتم عرض إدخال كلمة المرور على الشاشة. بمجرد تحديث كلمة المرور بنجاح، أعد تشغيل النظام باستخدام أمر `reboot`.

## الخلاصة

بعد إكمال العملية، يجب أن تكون قد قمت بتعيين كلمة مرور جديدة للمستخدم المطلوب بنجاح. يمكنك الآن استخدامها لتسجيل الدخول مرة أخرى عبر اتصال سطح المكتب البعيد. لأي أسئلة أو مساعدة إضافية، لا تتردد في التواصل مع فريق الدعم لدينا، المتوفر يوميًا لمساعدتك! 🙂