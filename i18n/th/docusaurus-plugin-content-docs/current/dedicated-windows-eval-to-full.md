---
id: dedicated-windows-eval-to-full
title: "เซิร์ฟเวอร์เฉพาะ: แปลง Windows Server Evaluation เป็นเวอร์ชันเต็ม"
description: "ค้นหาวิธีแปลง Windows Server Evaluation เป็นเวอร์ชันเต็ม → เรียนรู้เพิ่มเติมตอนนี้"
sidebar_label: แปลง Eval เป็นเต็ม
services:
  - dedicated
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import InlineVoucher from '@site/src/components/InlineVoucher';

## แนะนำ

Windows Server รุ่น Evaluation ถูกออกแบบมาเพื่อการทดสอบและมีข้อจำกัดเรื่องระยะเวลา เพื่อใช้งานในสภาพแวดล้อมจริงต่อไป คุณสามารถแปลงรุ่น Evaluation ให้เป็นเวอร์ชันที่มีไลเซนส์เต็มโดยไม่ต้องติดตั้งระบบปฏิบัติการใหม่

Microsoft รองรับการแปลงในที่เดียวกันนี้ผ่านเครื่องมือ DISM โดยต้องมีรหัสผลิตภัณฑ์ที่ถูกต้องสำหรับรุ่นเป้าหมาย



<InlineVoucher />



## สิ่งที่ต้องเตรียม

ก่อนเริ่มแปลง ให้แน่ใจว่าคุณมีรหัสผลิตภัณฑ์ที่ถูกต้องสำหรับ Windows Server รุ่นที่ต้องการเปลี่ยน เช่น Standard หรือ Datacenter รหัสผลิตภัณฑ์ต้องตรงกับรุ่นเป้าหมายอย่างแม่นยำ



## ตรวจสอบรุ่นปัจจุบัน

เพื่อยืนยันว่าติดตั้งรุ่นไหนอยู่ ให้เปิด Command Prompt หรือ PowerShell ในโหมดผู้ดูแลระบบ แล้วรันคำสั่งนี้:

```powershell
DISM /Online /Get-CurrentEdition
```

ผลลัพธ์จะแสดงรหัสรุ่นที่ใช้งานอยู่ โดยรุ่น Evaluation จะขึ้นเป็น `ServerStandardEval` หรือ `ServerDatacenterEval`



## ตรวจสอบรุ่นเป้าหมายที่รองรับ

ไม่ใช่ทุกรุ่นที่จะสามารถแปลงเป็นรุ่นอื่นได้ทั้งหมด เพื่อดูว่ารุ่นที่มีไลเซนส์รองรับสำหรับการแปลงในเครื่องคุณมีอะไรบ้าง ให้รันคำสั่งนี้:

```
DISM /Online /Get-TargetEditions
```

รายชื่อรุ่นที่แสดงคือรุ่นเป้าหมายที่ถูกต้องและสามารถใช้สำหรับการแปลงได้



## อัปเกรดจาก Evaluation เป็นเวอร์ชันเต็ม

เมื่อรู้รุ่นเป้าหมายและมีรหัสผลิตภัณฑ์ที่ถูกต้องแล้ว ก็สามารถเริ่มอัปเกรดได้ ค่า `<TargetEdition>` ต้องตรงกับหนึ่งในรุ่นที่รองรับที่ได้จากคำสั่งตรวจสอบรุ่นเป้าหมาย

รุ่นเป้าหมายที่พบบ่อยได้แก่:
- `ServerStandard`
- `ServerDatacenter`

ใช้คำสั่งนี้เพื่อเริ่มแปลง โดยแทนที่ `<TargetEdition>` ด้วยรุ่นที่ต้องการ และ `<ProductKey>` ด้วยรหัสผลิตภัณฑ์ Windows Server 25 ตัวอักษรของคุณ:


```
DISM /Online /Set-Edition:<TargetEdition> /ProductKey:<ProductKey> /AcceptEula
```

| รุ่นระบบปฏิบัติการ           | รหัสไลเซนส์ Volume License ทั่วไป  |
| ------------------------------ | ---------------------------------- |
| Windows Server 2025 Standard   | TVRH6-WHNXV-R9WG3-9XRFY-MY832     |
| Windows Server 2025 Datacenter | D764K-2NDRG-47T6Q-P8T8W-YP6DF     |
| Windows Server 2022 Standard   | VDYBN-27WPP-V4HQT-9VMD4-VMK7H     |
| Windows Server 2022 Datacenter | WX4NM-KYWYW-QJJR4-XV3QB-6VM33     |

ในระหว่างกระบวนการ อาจดูเหมือนว่าการแปลงหยุดชั่วคราวซึ่งเป็นเรื่องปกติ หลังจากแปลงเสร็จแล้ว จำเป็นต้องรีสตาร์ทเครื่องเพื่อให้การเปลี่ยนรุ่นสมบูรณ์



## ตรวจสอบเวอร์ชัน Windows Server

หลังรีสตาร์ท เปิด Command Prompt หรือ PowerShell ในโหมดผู้ดูแลระบบ แล้วรัน:

```powershell
DISM /Online /Get-CurrentEdition
```

ผลลัพธ์จะแสดงรุ่นที่มีไลเซนส์ เช่น `ServerStandard` หรือ `ServerDatacenter` เพื่อยืนยันว่าการแปลงจากรุ่น Evaluation สำเร็จแล้ว

ในขั้นตอนนี้ คุณจะเห็นลายน้ำ **Activate Windows** ที่มุมล่างขวาของหน้าจอ คุณสามารถใช้ไลเซนส์ Windows Server ที่ซื้อมาเพื่อเปิดใช้งาน Windows Server Standard หรือ Datacenter ได้เลย



## สรุป

ยินดีด้วย! ตอนนี้คุณแปลง Windows Server จากรุ่น EVAL เป็นเวอร์ชันเต็มสำเร็จแล้ว หากมีคำถามหรือขอความช่วยเหลือเพิ่มเติม ติดต่อทีมซัพพอร์ตของเราได้เลย พร้อมให้บริการทุกวัน 🙂



<InlineVoucher />