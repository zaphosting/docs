name: Validate JSONs

on: [pull_request]

jobs:
  verify-json-validation:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v3
      - name: Validate JSON
        uses: docker://orrosenblatt/validate-json-action:latest
        env:
          INPUT_SCHEMA: "/.github/workflows/schema.json"
          INPUT_JSONS: /sidebars.json,/sprachvariablen/en.json,/sprachvariablen/de.json
